import { env } from "cloudflare:workers";import { requireApiUser } from "../../../lib/server-auth";
export async function GET(request:Request){const user=await requireApiUser(request);if(!user)return Response.json({error:"Unauthorized"},{status:401});const rows=await env.DB.prepare("SELECT * FROM posts ORDER BY created_at DESC LIMIT 50").all();return Response.json(rows.results)}
export async function POST(request:Request){const user=await requireApiUser(request);if(!user)return Response.json({error:"Unauthorized"},{status:401});const {communityId,body}=await request.json() as {communityId?:string;body?:string};if(!body?.trim())return Response.json({error:"Post required"},{status:400});const id=crypto.randomUUID();await env.DB.prepare("INSERT INTO posts (id,community_id,author_id,body,created_at) VALUES (?,?,?,?,?)").bind(id,communityId||"creative-circle",user.id,body.trim(),Date.now()).run();return Response.json({id,body:body.trim()},{status:201})}
