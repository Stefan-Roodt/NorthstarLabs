"use client";import{useState}from"react";const lessons=["Welcome to the lab","Find your sharpest positioning","Build a memorable point of view","Design your brand system","Create your launch narrative","Final project & certificate"];export default function Learn(){const [current,setCurrent]=useState(0);const progress=Math.round((current+1)/lessons.length*100);return <main className="learn-page"><header><a className="system-brand" href="/dashboard">✦ NORTHSTARLABS</a><div><span>{progress}% complete</span><i><b style={{width:`${progress}%`}}/></i></div><button>Course menu</button></header><div className="learn-layout"><aside><p className="sys-kicker">BRAND STRATEGY LAB</p><h2>Your curriculum</h2>{lessons.map((l,i)=><button key={l} className={i===current?"active":i<current?"done":""} onClick={()=>setCurrent(i)}><span>{i<current?"✓":i+1}</span>{l}</button>)}</aside><section><div className="video-placeholder"><div>▶</div><span>LESSON {current+1} · 18:42</span></div><p className="sys-kicker">MODULE {current+1}</p><h1>{lessons[current]}</h1><p className="lesson-copy">Build a brand people recognize, remember, and choose. In this lesson, you’ll turn your expertise into a clear strategic advantage through practical examples and a guided workbook.</p><div className="lesson-actions"><button disabled={current===0} onClick={()=>setCurrent(current-1)}>← Previous</button><button className="sys-primary" disabled={current===lessons.length-1} onClick={()=>setCurrent(current+1)}>Complete & continue →</button></div></section></div></main>}
